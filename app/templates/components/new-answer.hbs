{{#if addNewAnswer}}
<h1>Your answer</h1>
  <div>
    <form>
      <div class="form-group">
        <label for="answer">answer</label>
        {{input value=answer id="answer"}}
      </div>
      <button {{action 'saveAnswer'}}>Save</button>
    </form>
  </div>
{{else}}
  <button {{action 'answerFormShow'}}>Answer</button>
{{/if}}
